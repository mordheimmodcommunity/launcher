import { styled } from '@material-ui/core/styles'
import { Box } from '@material-ui/core'

export const ModsContainer = styled(Box)({
  display: 'flex',
  flex: 1,
  flexDirection: 'column',
  justifyContent: 'space-evenly',
  alignItems: 'center',
  height: '100%',
  width: '100%',
})
